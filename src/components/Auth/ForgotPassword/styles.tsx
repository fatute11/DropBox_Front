import { createStyles, Theme } from "@material-ui/core";

export type Styles = "forgotPassword";
const styles = (theme: Theme) => createStyles<Styles, {}>({
    forgotPassword:{

    }
});

export default styles;